# ABIs

Place protocol ABIs here for:

- `SplitRiskPoolFactory`
- `SplitRiskPool`
- `YSGovernor`

These should be copied from the main YieldShield monorepo deployment artifacts
when the processor is wired up.
